# Reset bindkey to "emacs". Why is this necessary?
#
# $ man zshzle
#
# > If one of the VISUAL or EDITOR environment variables
# > contain the string `vi' when the shell starts up
# > then it will be `viins', otherwise it will be `emacs'.

bindkey -e
